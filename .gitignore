node_modules/
dist/
.vite/
results/
*.tsbuildinfo
.DS_Store
.gstack/

# Heavy source-art binaries (keep the PNG packs, skip the bloat)
mapgen/UnityPackages/
*.unitypackage
*.psd
mapgen/**/PSD/
mapgen/**/Coupon*
mapgen/**/*.pdf
mapgen/**/*.url
mapgen/**/*.txt

# Raw imported asset packs (Evo UI, sound packs) — vendored selections live in apps/web/public
imported_assets/

# Raw 3D model exports (~800 MB of GLBs) — optimized copies are vendored into
# apps/web/public/voxel3d/models by scripts/optimize-3d-assets.sh
assets/

# Ripped reference SFX (Blizzard IP — local prototyping only, never commit)
apps/web/public/audio/starcraft/
